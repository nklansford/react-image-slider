import React from 'react';

function ImageSlider(props) {
  const { images } = props;
  return (
    <div>
      {images};
    </div>
  );
}

export default ImageSlider;


// props {
//   name: "Noelle"
// }
